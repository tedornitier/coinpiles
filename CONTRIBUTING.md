# Contributing

## Setup

Clone the repo and install in editable mode so local changes take effect immediately:

```bash
git clone https://github.com/your-username/coinpiles.git
cd coinpiles
pip install -e .
```

## Guidelines

- Prefer small, focused pull requests.
